@model List<MiniStop.Models.LoaiSanPham>
<ul class="list-group list-group-flush">
    <li href="#" class="list-group-item p-0 text-white text-uppercase font-weight-bold rounded bg-success pl-2">
        <img src="images/beach_ball_24px.png" alt="" class="img-menu-type"> danh mục sản phẩm
    </li>
    @foreach (var item in Model)
    {
        <li>
            <a href="#" class="list-group-item bg-transparent text-white border-0">@item.TenLoai</a>
        </li>
    }

</ul>